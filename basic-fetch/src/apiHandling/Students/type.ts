export interface Student {
  id: string;
  studentName: string;
  age: number;
  Major: string;
}

export interface Course {
  id: string;
  courseName: string;
  instructor: string;
  maxStudents: number;
}
# Restaurant Sync vs Async Demo - FastAPI Backend

## Run

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

Open Swagger:

```text
http://localhost:8000/docs
```

## Endpoints

- `POST /sync-demo`
- `POST /async-demo`
